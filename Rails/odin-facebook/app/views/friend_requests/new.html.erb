<%= form_for @friend_request do |f| %>
  <%= f.hidden_field :sender_id %>
  <%= f.hidden_field :receiver_id %>
  <%= f.hidden_field :acceptance %>
  <%= f.submit "Create this friend request?" %>
<% end %>